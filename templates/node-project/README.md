# Node 项目模板

最小可用的 Node.js 项目骨架（无额外依赖），用于配合 `harness_skill` 跑通：`npm test`、`npm run lint`（可选）等流程。

## 使用

```bash
cp -r templates/node-project/* /path/to/your/project/
```

## 约定

- 源码：`src/`
- 测试：`tests/`


