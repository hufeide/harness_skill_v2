# Python 项目模板

最小可用的 Python 项目骨架（标准库测试），适合配合 `harness_skill` 跑通 `src/` + `tests/` 约定。

## 使用

```bash
cp -r templates/python-project/* /path/to/your/project/
```

## 运行测试

```bash
python -m unittest -v
```


