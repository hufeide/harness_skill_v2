# Ruby 项目模板

最小可用的 Ruby 项目骨架（RSpec），用于配合 `harness_skill` 跑通 `src/` + `tests/`（Ruby 通常用 `spec/`）的约定。

## 使用

```bash
cp -r templates/ruby-project/* /path/to/your/project/
bundle install
bundle exec rspec
```


